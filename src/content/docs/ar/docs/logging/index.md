---
title: "التسجيل"
sidebar:
  order: 7
---

يتضمن Gin وسيط تسجيل مدمج يسجّل تفاصيل حول كل طلب HTTP، بما في ذلك رمز الحالة وطريقة HTTP والمسار والتأخير.

عند إنشاء موجّه باستخدام `gin.Default()`، يُرفق وسيط التسجيل تلقائياً مع وسيط الاسترداد:

```go
// Logger and Recovery middleware are already attached
router := gin.Default()
```

إذا كنت تحتاج التحكم الكامل في الوسيطات المستخدمة، أنشئ موجّهاً باستخدام `gin.New()` وأضف المسجّل يدوياً:

```go
// No middleware attached
router := gin.New()

// Attach the logger middleware
router.Use(gin.Logger())
```

يكتب المسجّل الافتراضي إلى `os.Stdout` وينتج مخرجات مثل هذه لكل طلب:

```
[GIN] 2025/06/01 - 15:04:05 | 200 |     512.345µs |       127.0.0.1 | GET      "/ping"
```

يتضمن كل إدخال طابعاً زمنياً ورمز حالة HTTP وتأخير الطلب وعنوان IP للعميل وطريقة HTTP والمسار المطلوب.

## في هذا القسم

- [**كتابة السجلات إلى ملف**](./write-log/) -- إعادة توجيه مخرجات السجل إلى ملف أو وحدة التحكم أو كليهما في نفس الوقت.
- [**تنسيق سجل مخصص**](./custom-log-format/) -- تحديد تنسيق السجل الخاص بك باستخدام `LoggerWithFormatter`.
- [**تخطي التسجيل**](./skip-logging/) -- تخطي التسجيل لمسارات أو شروط محددة.
- [**التحكم في تلوين مخرجات السجل**](./controlling-log-output-coloring/) -- تفعيل أو تعطيل مخرجات السجل الملونة.
- [**تجنب تسجيل سلاسل الاستعلام**](./avoid-logging-query-strings/) -- إزالة معاملات الاستعلام من مخرجات السجل للأمان والخصوصية.
- [**تحديد تنسيق سجل المسارات**](./define-format-for-the-log-of-routes/) -- تخصيص كيفية طباعة المسارات المسجلة عند بدء التشغيل.
