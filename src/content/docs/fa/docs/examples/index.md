---
title: "🍳 دستور العمل‌های Gin (کتاب آشپزی)"
sidebar:
  order: 6
---

## مقدمه

این بخش نشان می‌دهد که **چگونه از Gin در کد خود استفاده کنید** از طریق دستور العمل‌های کوچک و عملی.
هر دستور العمل روی یک **مفهوم واحد** تمرکز دارد تا بتوانید سریع یاد بگیرید و بلافاصله آن را به کار ببرید.

از این مثال‌ها به عنوان مرجع برای ساختاردهی API‌های واقعی با استفاده از Gin استفاده کنید.

---

## 🧭 آنچه یاد خواهید گرفت

در این بخش، مثال‌هایی را خواهید یافت که موارد زیر را پوشش می‌دهند:

- **مبانی سرور**: اجرای سرور، مسیریابی و پیکربندی.
- **مدیریت درخواست**: اتصال داده‌های JSON، XML و فرم.
- **میان‌افزار**: استفاده از میان‌افزار داخلی و سفارشی.
- **رندرینگ**: ارائه HTML، JSON، XML و بیشتر.
- **امنیت**: مدیریت SSL، هدرها و احراز هویت.

---

## 🥇 دستور العمل 1: سرور کمینه Gin

**هدف:** راه‌اندازی یک سرور Gin و مدیریت یک درخواست اولیه.

### مراحل

1. ایجاد یک روتر
2. تعریف یک مسیر
3. راه‌اندازی سرور

```go
package main

import "github.com/gin-gonic/gin"

func main() {
  r := gin.Default()

  r.GET("/ping", func(c *gin.Context) {
    c.JSON(200, gin.H{
      "message": "pong",
    })
  })

  r.Run(":8080") // http://localhost:8080
}
```
